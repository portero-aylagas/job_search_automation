name: Verify

on:
  pull_request:
  push:

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ""
      ANTHROPIC_API_KEY: ""
      GOOGLE_API_KEY: ""
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            python -m pip install -r requirements.txt
          fi
          if [ -f requirements-dev.txt ]; then
            python -m pip install -r requirements-dev.txt
          fi
          if [ -f requirements-test.txt ]; then
            python -m pip install -r requirements-test.txt
          fi
          if [ -f pyproject.toml ] && grep -q '^\[project\]' pyproject.toml; then
            python -m pip install -e .
          fi
          # If the target repo has tests but no dependency file installs pytest,
          # either add pytest to the repo's dev/test dependencies or temporarily
          # add: python -m pip install pytest

      - name: Run verification
        run: make verify
