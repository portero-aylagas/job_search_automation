repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-json
      - id: check-yaml

# Optional ruff checks. Enable only after the project accepts linting as part of
# normal verification.
#  - repo: https://github.com/astral-sh/ruff-pre-commit
#    rev: v0.8.4
#    hooks:
#      - id: ruff
#        args: [--fix]
#      - id: ruff-format
